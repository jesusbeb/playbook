// Ejemplo 2: for Each para calcular la suma de todos los elementos de una lista
let sum = 0;    // se declara variable sum igual a cero
const numbers2 = [1, 2, 3, 4, 5];   // se declara una constante que contiene la lista
numbers2.forEach(num => sum += num)     // se realiza la suma
console.log("Ejemplo 2: Cálculo de la suma de los elementos de la lista")   // se imprime este texto
console.log(sum)    // se imprime el valor de la variable sum